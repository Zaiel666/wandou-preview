# 豌豆预览 0.2.0 测试记录

测试日期：2026-09-12，环境：Windows 11 x64、Cinema 4D 2023、Blender 4.2。

| 检查 | 结果 |
| --- | --- |
| GitHub Actions 构建 C#、C++、Shell DLL、单文件安装器和 ZIP | 通过 |
| 截图同名 `www_modown_cn_A31_06.fbx` 独立渲染 | 通过，512 × 512，显示椰树模型 |
| 复杂植物 OBJ 独立渲染 | 通过，512 × 512，显示完整静态网格 |
| Illustrator AI（259,842,112 字节）PDF 兼容预览 | 通过，351 × 512 |
| Radiance HDR 内置解码与自动曝光 | 通过，512 × 256 |
| Cinema 4D 椰子场景保存预览 | 通过，405 × 500 |
| Windows Shell 返回 FBX、OBJ、AI、HDR、C4D 缩略图 | 全部通过 |
| FBX、OBJ、HDR、C4D 左下角格式标识 | 已检查实际缩略图 |
| 旧 C4D Quick Preview 自动迁移 | 通过 |
| 旧版右键预览菜单升级时自动清理 | 通过 |
| PowerShell 安装器语法与当前用户安装 | 通过 |
| 截图中的 `www_modown_cn_S08_02.fbx` 直接渲染与 Shell 缩略图 | 通过，确认彩色水滴来自旧缓存 |
| 已安装 0.1.0 上执行同版本覆盖安装 | 通过，使用完整源路径比较文件哈希 |
| 重启后注册仍存在但 Explorer 沿用旧缓存 | 加入登录后一次性同步刷新，进程随后退出 |
| Blender `.blend` 内嵌预览读取与 Shell 缩略图 | 通过真实 Blender 3.04 文件验证 |
| MP4、AVI、MOV、MKV、WebM 等视频关联 | 通过，均使用独立的视频封面后台组件 |
| HEVC/H.265 MP4 封面提取 | 通过真实 34,921,360 字节文件验证，Windows Shell 返回 256 × 145 画面 |
| FFmpeg 后备解码及左下角视频格式标识 | 通过，HEVC 文件显示正确内容和 `MP4` 标识 |
| 近期 23MB、104MB、113MB FBX 的 Windows Shell 缩略图 | 全部通过 |
| FBX 7500 大型植物模型 | 314MB 文件通过，Windows Shell 约 13 秒返回 256 × 256 缩略图 |
| 大型模型后台资源限制 | 模型等待上限 60 秒、进程内存上限 3GB |
| 同版本升级时 Shell DLL 被占用 | 通过内容哈希目录避开锁定文件，无需重启 Windows |
| 单文件安装器调用系统 Windows PowerShell | 通过，哈希计算不依赖 `Get-FileHash` cmdlet |

测试使用用户电脑上已有的真实模型、图片和视频，文件未上传到仓库。通用模型目前以静态网格和基础材质颜色为主；外部贴图、动画、程序化材质和第三方渲染节点仍可能简化。未对所有格式版本及 GB 级文件做压力测试。
