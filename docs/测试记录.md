# 0.1.0 测试记录

测试日期：2026-09-10。

| 检查 | 结果 |
| --- | --- |
| GitHub Windows x64 原生 DLL 和 .NET 窗口编译 | 通过 |
| C4D 2023 `car_rig.c4d` 直接读取保存预览 | 通过，500 × 269 图片 |
| C4D 2023 `FaceRig.c4d` 直接读取保存预览 | 通过，500 × 269 图片 |
| 无效内容的 `.c4d` 文件 | 工作进程返回失败，不输出假预览 |
| Windows Shell `IShellItemImageFactory` 读取真实场景缩略图 | 通过，256 × 137 图片 |
| 缩略图左下角 C4D 文字标识 | 已检查实际返回图片，可见且未铺满画面 |
| Windows PowerShell 5.1 完整安装流程 | 通过 |
| 卸载后逐项核对原注册值 | 通过 |

测试使用本机 C4D 2023 自带组件和示例场景。未将 Maxon 组件或示例文件上传到仓库或打包分发。

测试结束后已卸载工具注册，保留安装文件。用户下载 ZIP 后自行安装即可。

本记录不代表已验证所有 C4D 版本、所有大场景或手工点击资源管理器的全部交互。尚未进行 GB 级场景压力测试，右键窗口的滚轮和拖动交互仍需要实际使用反馈。本版没有三维旋转、HDR 或其他三维格式支持。
